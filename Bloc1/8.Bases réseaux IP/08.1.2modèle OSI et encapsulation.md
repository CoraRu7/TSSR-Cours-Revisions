

## 🧠 **Titre : Modèles OSI, TCP/IP et principe d’encapsulation**

### 🎯 **Objectif**

Comprendre les **caractéristiques** des modèles **OSI** et **TCP/IP**, ainsi que le **principe d’encapsulation** des données dans les communications réseau.

---

### 🧩 **Concepts clés**

#### 🔹 Modèle OSI (ISO)

Modèle théorique à **7 couches** permettant de standardiser la communication réseau :

1. **Physique** – Transmission du signal (électrique, optique, radio).
2. **Liaison** – Détection d’erreurs, adressage physique (MAC).
3. **Réseau** – Routage et adressage logique (IP).
4. **Transport** – Transmission fiable des données (TCP/UDP).
5. **Session** – Ouverture, maintien et fermeture de la connexion.
6. **Présentation** – Formatage et chiffrement des données.
7. **Application** – Interface avec les logiciels (HTTP, FTP, etc.).

---

#### 🔹 Modèle TCP/IP

Modèle **pratique** utilisé sur Internet, composé de **4 couches** :

1. **Accès réseau** (physique + liaison)
2. **Internet** (routage IP)
3. **Transport** (TCP, UDP)
4. **Application** (HTTP, FTP, DNS, etc.)

➡️ Le modèle TCP/IP regroupe certaines couches du modèle OSI.

---

### 🧱 **Principe d’encapsulation**

* À l’envoi : chaque couche **ajoute un en-tête** au message → création d’une **unité de données** adaptée à la couche.
* À la réception : chaque couche **lit puis retire** son en-tête → on parle alors de **décapsulation**.

| **Couche**  | **Nom du PDU (unité de données)** |
| ----------- | --------------------------------- |
| Application | Message                           |
| Transport   | Segment                           |
| Réseau      | Paquet                            |
| Liaison     | Trame                             |
| Physique    | Signal                            |

---

### 💡 **À retenir**

* **OSI = modèle théorique (7 couches)** / **TCP-IP = modèle réel (4 couches)**.
* **Encapsulation = ajout d’en-têtes successifs** pour garantir la transmission.
* **Décapsulation = suppression inverse à la réception.**

---
