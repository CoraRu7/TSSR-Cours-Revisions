## 🧾 **Titre : Création et gestion de clés RSA avec OpenSSL**

### 🎯 **Objectif :**

Comprendre le fonctionnement de l’algorithme RSA et savoir générer, chiffrer et exporter des clés RSA (publique et privée) à l’aide de l’outil **OpenSSL** sous Linux/Debian.

---

### 🧠 **Concepts clés :**

* **RSA (Rivest–Shamir–Adleman, 1977)** : algorithme de chiffrement **asymétrique** utilisant deux clés :

  * **Clé publique** → pour chiffrer les données
  * **Clé privée** → pour déchiffrer les données
* Utilisé dans de nombreux protocoles : **SSH**, **SSL**, **TLS**, **HTTPS**
* Les clés sont généralement de **1024 à 2048 bits** (les entreprises préfèrent 2048 bits).
* Format de clé **PEM** (*Privacy Enhanced Mail*) : encodage en Base64, très courant dans les systèmes Linux.
* L’**asymétrie** résout le problème d’échange de clé de la cryptographie symétrique.
* La **passphrase** protège la clé privée contre une utilisation non autorisée.

---

### 💻 **Commandes & Notes :**

| Étape                                     | Commande                                                 | Description                                                                               |
| ----------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| **1. Génération d’une paire de clés RSA** | `openssl genrsa -out toto.pem 1024`                      | Crée la clé privée `toto.pem` (clé RSA de 1024 bits).                                     |
| **2. Afficher la clé privée**             | `cat toto.pem`                                           | Affiche le contenu complet de la clé privée RSA.                                          |
| **3. Visualiser les détails de la clé**   | `openssl rsa -in toto.pem -text -noout`                  | Décode la clé RSA et affiche ses informations sans la réécrire dans le fichier.           |
| **4. Chiffrer la clé privée**             | `openssl rsa -in toto.pem -des3 -out toto.pem`           | Protège la clé privée avec un mot de passe en utilisant l’algorithme symétrique **DES3**. |
| **5. Exporter la clé publique**           | `openssl rsa -in toto.pem -pubout -out totoPublique.pem` | Génère la clé publique à partir de la clé privée chiffrée.                                |
| **6. Vérifier la clé publique**           | `openssl rsa -in totoPublique.pem -pubin`                | Vérifie que la clé publique a bien été exportée et affiche son contenu.                   |

---

### ⚠️ **Essentiel à retenir :**

* Toujours **protéger la clé privée** avec une **passphrase**.
* Ne **jamais partager** la clé privée (`toto.pem`).
* La clé publique (`totoPublique.pem`) peut être diffusée pour le chiffrement ou l’authentification.
* Respecter l’ordre des étapes : génération → protection → exportation → vérification.
* Les algorithmes RSA sont robustes, mais la sécurité dépend aussi de la **longueur de clé** et de la **qualité du mot de passe**.

---

### 🔗 **Liens utiles :**

* [Documentation OpenSSL officielle](https://www.openssl.org/docs/)
* [Guide OpenSSL – clés RSA et certificats](https://wiki.openssl.org/index.php/Command_Line_Utilities)
* [Cours sur la cryptographie asymétrique – ANSSI](https://www.ssi.gouv.fr/)
