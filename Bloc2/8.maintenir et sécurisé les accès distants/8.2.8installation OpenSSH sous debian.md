# 🧩 **Titre : Installation et configuration d’OpenSSH sous Debian 11**

---

## 🎯 **Objectif**

Installer, configurer et sécuriser le service OpenSSH sur Debian 11 afin de permettre l’administration à distance d’un serveur de manière chiffrée et sécurisée.

---

## 🧠 **Concepts clés**

* **SSH (Secure Shell)** : protocole de communication sécurisée pour accéder à un système distant (chiffrement des données et authentification).
* **OpenSSH** : implémentation libre et open source du protocole SSH, intégrée dans Debian.
* **Connexion sécurisée** : protège les identifiants et les commandes contre l’interception.
* **Daemon SSHD** : service côté serveur qui écoute les connexions entrantes SSH.
* **Port par défaut : 22** (modifiable pour plus de sécurité).
* **Authentification** : par mot de passe ou par clé publique/privée.

---

## ⚙️ **Commandes et notes**

### 🏗️ **Installation d’OpenSSH sous Debian 11**

1. **Installer `sudo` si nécessaire**

   ```bash
   apt-get install sudo
   ```

2. **Configurer les droits sudo**

   ```bash
   nano /etc/sudoers
   ```

3. **Mettre à jour le système**

   ```bash
   sudo apt update
   ```

4. **Installer le serveur SSH**

   ```bash
   sudo apt-get install openssh-server
   ```

5. **Démarrer le service SSH**

   ```bash
   sudo systemctl start ssh
   ```

6. **Activer le service au démarrage**

   ```bash
   sudo systemctl enable ssh
   ```

7. **Vérifier le statut du service**

   ```bash
   sudo systemctl status ssh
   ```

8. **Trouver l’adresse IP du serveur**

   ```bash
   ip a
   ```

---

### 💻 **Connexion SSH depuis un autre poste**

#### 🪟 Via PuTTY (Windows)

* Ouvrir **PuTTY**
* Entrer l’**adresse IP** du serveur et le **port 22**
* Accepter la clé d’hôte lors de la première connexion
* Entrer :

  * Nom d’utilisateur
  * Mot de passe

#### 🧰 Via le client OpenSSH (Windows PowerShell)

1. **Installer le client SSH**

   ```bash
   Add-WindowsCapability –Online –Name OpenSSH.Client~~~~0.0.1.0
   ```
2. **Vérifier l’installation**

   ```bash
   Get-WindowsCapability –Online –Name OpenSSH.Client~~~~0.0.1.0
   ```
3. **Connexion au serveur Debian**

   ```bash
   ssh utilisateur@adresseIP
   # Exemple :
   ssh cec@192.168.94.5
   ```

---

## 🔒 **Sécurisation du service SSH**

### Bonnes pratiques :

* **Désactiver l’accès root** :

  ```bash
  PermitRootLogin no
  ```
* **Utiliser uniquement le protocole 2** :

  ```bash
  Protocol 2
  ```
* **Modifier le port d’écoute (ex : 5555)** :

  ```bash
  Port 5555
  ```

  → Connexion :

  ```bash
  ssh -p5555 user@adresseIP
  ```
* **Limiter les utilisateurs autorisés** :

  ```bash
  AllowUsers cec
  ```
* **Réduire le temps d’authentification (30s)** :

  ```bash
  LoginGraceTime 30
  ```
* **Vérifier régulièrement les logs** :

  ```bash
  cat /var/log/auth.log
  ```

---

## 🔧 **Fichier de configuration principal**

```bash
sudo nano /etc/ssh/sshd_config
```

Après chaque modification :

```bash
sudo systemctl restart sshd
```

---

## 🔗 **Liens utiles**

* [Documentation officielle Debian SSH](https://wiki.debian.org/SSH)
* [Manuel OpenSSH](https://man.openbsd.org/sshd)
* [PuTTY Download](https://www.putty.org/)

---